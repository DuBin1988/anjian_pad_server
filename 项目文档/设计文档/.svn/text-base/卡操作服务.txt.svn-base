1. 建立接口dll，各厂家根据接口dll生成自己的dll。做不到的，由我们来做。

2. 建立卡操作服务，卡操作服务从后台自动下载变化了的动态库等相关文件。

3. 各种dll在服务器端统一部署，布置在后台resourdces的card目录下。
服务开始后，自动到服务器端下载。

4. 后台与卡相关的服务根路径为card。

GET

获取所以文件目录及其最后修改时间。

PATH

/{filename} - 下载给定名称文件。