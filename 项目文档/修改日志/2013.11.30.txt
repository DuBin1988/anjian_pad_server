1. 卡对象增加写卡前事件，以便写卡前可以获取一些值。

2. hibernate配置变化

2.1 sellinggas增加了与补气记录之间的对一关系。
2.2 fillgas，补气记录增加了是否有效标记。
2.3 userfiles，增加了最后写入卡里的气量，以便初始化卡购气用。

3. 页面

3.1 补气页面添加了，补气后初始化状态还原功能。以及保存最后写入卡里的气量功能。
3.2 增加了换表后购气不成功的冲正功能
3.3 换表后初始化状态一定为是
3.4 售气写卡自动带剩余气量，并将最后写入卡里的气量保存到用户档案里。
3.5 初始化购气修改成，从用户档案里取最后一次写入卡里的气量了。